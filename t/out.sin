import Iter from "sino.iterable"
import Arr from "sino.array"

func x()
    return {a = 1, b = 2}
end

let a = x()

let {a: x, b: y} = a

{y,x}
|> Arr:sort(func(a, b) => a < b)
|> Arr:each(func(val, idx) => print(val))

Arr:each(Arr:sort({y,x}, func(a,b) => a < b), func(val, idx) => print(val))